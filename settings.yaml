# docker-compose.yaml の設定

docker_settings:
  nginx:
    use: true
    parse_conf: false
  express:
    use: true
  python:
    use: true
  mysql:
    use: true
  php-apache:
    use: false
  php-fpm:
    use: false
  phpmyadmin:
    use: true
