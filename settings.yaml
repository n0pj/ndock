# docker-compose.yaml の設定

docker_settings:
  nginx:
    use: true
    parse_conf: true
  express:
    use: true
  mysql:
    use: false
  php-apache:
    use: false
  php-fpm:
    use: false
  phpmyadmin:
    use: false
